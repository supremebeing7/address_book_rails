<h1>Wikipages</h1>

<ul>
  <% @contacts.sort_by(&:name).each do |contact| %>
    <li><a href="/contacts/<%= contact.id %>"><%= contact.name %></a></li>
  <% end %>
</ul>

<a href="/contacts/new">New contact</a></p>

